<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Nos testes em meu celular Android 16, vi que o sistema cega os aplicativos
        para que ele não tenha conhecimento de outros apps. Isso me retornava Null
        ao tentar abrir URL com intent implícita.
        Para resolver, essa querie abaixo é necessária para que o sistema saiba que
        eu preciso ter conhecimento da existência de alguma Acitivty que possa resolver
        essa intent.-->
    <queries>
        <!-- Para URLs-->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>

        <!-- Para Emails-->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <!-- O esquema mailto é o que define que buscamos apps de e-mail -->
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_bovine"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_bovine_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gadiario">
        <activity
            android:name=".AboutActivity"
            android:exported="false" />
        <activity
            android:name=".BovinesActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BovineActivity"
            android:exported="true"></activity>
    </application>

</manifest>